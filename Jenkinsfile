pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                sh -c {'echo This is Build'}
            }
        }
        stage ('Test') {
            steps {
                sh -c {'echo This is Test'}
            }
        }
        stage ('Deploy') {
            steps {
                sh -c {'echo This is Deploy'}
            }
        }
    }
}
